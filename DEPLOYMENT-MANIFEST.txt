B232.11 — HOTFIX CALENDARIO
Diagnóstico:
La captura sigue mostrando el DOM del calendario legacy de app.js:
- calendar-grid vacío
- métricas legacy
- mensaje legacy
Esto demuestra que B232 V2 no está reemplazando #calendario en runtime.

Solución robusta:
1. B232.11 deja de bloquear la navegación con stopImmediatePropagation.
2. B219 carga B232.11 con cache-buster nuevo.
3. app.js, que es el controlador definitivo de pestañas, dispara explícitamente B232Calendario.load() después de abrir Calendario.
4. index.html invalida cache de B219 y app.js.

La lógica financiera de B232 no se modifica.
El objetivo es garantizar que, una vez abierta la pestaña, B232 sea el renderer efectivo y no el calendario legacy.

QA:
- Al abrir Calendario debe verse "B232 · CALENDARIO FINANCIERO · 232.11".
- Deben existir las 42 celdas del mes.
- Deben funcionar anterior/hoy/siguiente.
- Debe funcionar selección de día.
- Si una consulta Supabase falla, el renderer debe seguir mostrando la cuadrícula.
- Volver a Resumen y volver a Calendario debe volver a ejecutar B232.load().
