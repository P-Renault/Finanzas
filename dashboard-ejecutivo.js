/* Dashboard Ejecutivo — Visual Analytics Engine v1.2.5 */
window.ExecutiveDashboard = (() => {
  const NS='http://www.w3.org/2000/svg';
  const n=v=>Number(v||0);
  const money=v=>n(v).toLocaleString('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0});
  const compact=v=>{const x=Math.round(n(v)); if(Math.abs(x)>=1e6)return '$'+(x/1e6).toFixed(1)+'M'; if(Math.abs(x)>=1e3)return '$'+Math.round(x/1e3)+'k'; return '$'+x.toLocaleString('es-CL');};
  const svg=(w,h)=>{const e=document.createElementNS(NS,'svg');e.setAttribute('viewBox',`0 0 ${w} ${h}`);e.setAttribute('width','100%');e.setAttribute('height','100%');return e;};
  const line=(s,x1,y1,x2,y2,c='chart-grid')=>{const e=document.createElementNS(NS,'line');[['x1',x1],['y1',y1],['x2',x2],['y2',y2]].forEach(([k,v])=>e.setAttribute(k,v));e.classList.add(c);s.appendChild(e);};
  const text=(s,x,y,v,c='chart-label')=>{const e=document.createElementNS(NS,'text');e.setAttribute('x',x);e.setAttribute('y',y);e.textContent=v;e.classList.add(c);s.appendChild(e);};
  const rect=(s,x,y,w,h,c='chart-bar')=>{const e=document.createElementNS(NS,'rect');e.setAttribute('x',x);e.setAttribute('y',y);e.setAttribute('width',Math.max(0,w));e.setAttribute('height',Math.max(0,h));e.classList.add(c);s.appendChild(e);};
  const path=(s,d,c='chart-path')=>{const e=document.createElementNS(NS,'path');e.setAttribute('d',d);e.classList.add(c);s.appendChild(e);};
  function range(vals){const min=Math.min(...vals,0),max=Math.max(...vals,0),span=Math.max(1,max-min);return{min,max,span};}
  function lineChart(el,rows,fn,title){el.innerHTML='';if(!rows.length){text(svg(760,300),10,30,'Sin datos');return;}const W=760,H=300,L=54,R=18,T=28,B=42,s=svg(W,H);el.appendChild(s);const vals=rows.map(fn),r=range(vals);for(let i=0;i<=4;i++){const y=T+(H-T-B)*i/4;line(s,L,y,W-R,y);text(s,5,y+4,compact(r.max-r.span*i/4),'chart-axis');}const pts=rows.map((x,i)=>{const px=L+(W-L-R)*(rows.length===1?0:i/(rows.length-1)),py=T+(H-T-B)*(r.max-vals[i])/r.span;return[px,py];});path(s,pts.map((p,i)=>(i?'L':'M')+p[0]+' '+p[1]).join(' '));pts.forEach((p,i)=>{if(i===0||i===pts.length-1||i%Math.max(1,Math.floor(pts.length/6))===0){const c=document.createElementNS(NS,'circle');c.setAttribute('cx',p[0]);c.setAttribute('cy',p[1]);c.setAttribute('r',3);c.classList.add('chart-dot');s.appendChild(c);}});text(s,L,17,title,'chart-title');}
  function flowChart(el,rows){el.innerHTML='';if(!rows.length)return;const W=760,H=300,L=44,R=18,T=30,B=42,s=svg(W,H);el.appendChild(s);const max=Math.max(1,...rows.flatMap(x=>[n(x.income),n(x.expense)])),base=H-B,slot=(W-L-R)/rows.length,bar=Math.min(16,slot*.28);line(s,L,base,W-R,base);rows.forEach((r,i)=>{const x=L+i*slot+slot/2,ih=n(r.income)/max*(H-T-B-10),eh=n(r.expense)/max*(H-T-B-10);rect(s,x-bar-2,base-ih,bar,ih,'chart-income');rect(s,x+2,base-eh,bar,eh,'chart-expense');if(i===0||i===rows.length-1||i%Math.max(1,Math.floor(rows.length/6))===0)text(s,x,H-12,String(r.date).slice(5),'chart-axis');});text(s,L,17,'Ingresos vs egresos','chart-title');}
  function donut(el,items){el.innerHTML='';const W=420,H=300,s=svg(W,H);el.appendChild(s);const total=items.reduce((a,x)=>a+x.value,0);if(!total){text(s,120,150,'Sin obligaciones cuantificadas','chart-empty');return;}const cx=145,cy=150,r=92,inner=54;let a=-Math.PI/2;items.slice(0,6).forEach((it,i)=>{const a2=a+it.value/total*Math.PI*2,la=a2-a>Math.PI?1:0,x1=cx+r*Math.cos(a),y1=cy+r*Math.sin(a),x2=cx+r*Math.cos(a2),y2=cy+r*Math.sin(a2),d=`M ${x1} ${y1} A ${r} ${r} 0 ${la} 1 ${x2} ${y2} L ${cx+inner*Math.cos(a2)} ${cy+inner*Math.sin(a2)} A ${inner} ${inner} 0 ${la} 0 ${cx+inner*Math.cos(a)} ${cy+inner*Math.sin(a)} Z`;path(s,d,`donut-${i%6}`);text(s,270,45+i*34,`${it.label}: ${compact(it.value)}`,'chart-legend');a=a2;});text(s,cx-35,145,compact(total),'chart-center');text(s,cx-35,166,'comprometido','chart-center-small');}
  function candles(el,rows){el.innerHTML='';if(!rows.length)return;const W=760,H=300,L=46,R=18,T=28,B=42,s=svg(W,H);el.appendChild(s);const groups=[];for(let i=0;i<rows.length;i+=7){const q=rows.slice(i,i+7);groups.push({date:q[0].date,open:n(q[0].openingBalance),close:n(q[q.length-1].closingBalance),high:Math.max(...q.map(x=>n(x.closingBalance)),n(q[0].openingBalance)),low:Math.min(...q.map(x=>n(x.closingBalance)),n(q[0].openingBalance))});}const r=range(groups.flatMap(x=>[x.open,x.close,x.high,x.low]));groups.forEach((g,i)=>{const x=L+(W-L-R)*(groups.length===1?0:i/(groups.length-1)),y=v=>T+(H-T-B)*(r.max-v)/r.span;line(s,x,y(g.high),x,y(g.low),'candle-wick');rect(s,x-7,Math.min(y(g.open),y(g.close)),14,Math.max(4,Math.abs(y(g.close)-y(g.open))),g.close>=g.open?'candle-up':'candle-down');if(i===0||i===groups.length-1)text(s,x,H-12,String(g.date).slice(5),'chart-axis');});text(s,L,17,'Velas de liquidez','chart-title');}
  function renderCharts({context,projection,state}){const root=document.getElementById('executive-charts');if(!root)return;const p=projection||[],flow=p.slice(0,30).map(x=>({date:x.date,income:n(x.assuredIncome)+n(x.projectedIncome)+n(x.plannedIncome),expense:n(x.mandatoryExpenses)+n(x.discretionaryExpenses)}));const obs={};(context.obligations||[]).forEach(x=>{const k=x.source==='cuotas_deuda'?'Cuotas de deuda':x.source==='compromisos'?'Compromisos':'Deudas';obs[k]=(obs[k]||0)+n(x.amount);});const donutRows=Object.entries(obs).map(([label,value])=>({label,value})).filter(x=>x.value>0).sort((a,b)=>b.value-a.value);const risk=p.find(x=>n(x.closingBalance)<n(context.minimumReserve));const cov=n(state.committedExpenses)>0?(n(state.availableBalance)+n(state.assuredIncome))/n(state.committedExpenses)*100:100;const lr=document.getElementById('exec-liquidity-reading');if(lr)lr.textContent=risk?`Riesgo desde ${risk.date}`:'Sin caída bajo la reserva';const or=document.getElementById('exec-obligation-reading');if(or)or.textContent=`${Math.round(cov)}% de cobertura`;const fr=document.getElementById('exec-flow-reading');if(fr)fr.textContent=`Neto 30 días: ${money(flow.reduce((s,x)=>s+x.income-x.expense,0))}`;lineChart(document.getElementById('chart-liquidity'),p.filter((_,i)=>i%3===0),x=>n(x.closingBalance),'Liquidez proyectada');flowChart(document.getElementById('chart-flow'),flow);donut(document.getElementById('chart-obligations'),donutRows);candles(document.getElementById('chart-candles'),p);}
  return {renderCharts,VERSION:'EXEC-DASH-V1.2.5'};
})();
